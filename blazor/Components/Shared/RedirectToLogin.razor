@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo(
            AuthenticationNavigation.BuildSignInUrl(Navigation.BaseUri, Navigation.Uri),
            forceLoad: true);
    }
}
